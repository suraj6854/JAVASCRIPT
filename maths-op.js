//numbers and math object
const num1=100;
const num2=50;

// let val;

// val = num1+num2
// val = num1-num2
// val = num1*num2
// val = num1/num2
// val = num1%num2

//math objects
// val=Math.PI
// val=Math.round(2.1)
// val=Math.ceil(2.1)
//val=Math.floor(2.6)
//val=Math.sqrt(9)
val=Math.abs(-25)
val=Math.pow(2,3)
val=Math.min(1,2,3,4,5)
val=Math.max(1,2,3,4,5)
val=Math.random()
val=Math.floor(Math.random()*20+1)
console.log(val);
